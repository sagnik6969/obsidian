- Sometimes we need an HTML element to wrap other elements.
- Only use div and span when there are no other attributes you can use.
#### div
- It is a block level element.
#### span
- It is an inline element.
